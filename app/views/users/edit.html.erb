<h2>Update Your Profile</h2>

<%= form_for current_user do |f| %>

    <p>
        <%= f.label :first_name %><br />
        <%= f.text_field :first_name, :class => 'field_box' %>
    </p>

    <p>
        <%= f.label :address %> (Street, City, State)<br />
        <%= f.text_field :address, :class => 'field_box' %>
    </p>

<!-- USE GEOCODER TO GET LATITUDE & LONGITUDE -->
<!--     <p>
        <%= f.label :latitude %>
        <%= f.number_field :latitude, :step => 'any', :class => 'field_box' %>
    </p>

    <p>
        <%= f.label :longitude %>
        <%= f.number_field :longitude, :step => 'any', :class => 'field_box' %>
    </p>
 -->
<!-- USE GEOCODER TO GET LATITUDE & LONGITUDE -->


    <p>
        <%= f.label "Mode" %>
        <%= f.select :role, [['Parent', 0],['Service Provider', 1],['Other',2]], {}, { :class => 'select_box' } %><br />
    </p>

    <%= f.submit "Submit changes", :class => 'submit_button' %>

<% end %>

<div class="nav_footer">
    <div class="nav_mid"><%= link_to "Home", root_path %></div>
</div>
